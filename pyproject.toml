[project]
name = "rebase-conflict-demo"
version = "0.1.0"
description = "Hands-on demo: hazards of rebasing shared/public branches"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
# Reserved; scripts intentionally stay dependency-free.
